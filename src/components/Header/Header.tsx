import { Box } from "@mui/material";

const Header = () => {
  return (
    <Box sx={{ my: 4 }}>
      <img src={"/LTWlogo.png"} alt={"Link The World"} width={"50%"} />
    </Box>
  );
};

export default Header;
